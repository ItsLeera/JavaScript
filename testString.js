// let str = 'JavaScript';

// console.log(str.length);

// console.log(str.charAt(0));
// console.log(str.charAt(1));

let strr = "hello world";
let upCase = strr.toUpperCase();

console.log(upCase)

let sentence = "i love coding in JavaScript";

// console.log(sentence.includes('coding'));

let text = "programming";

// console.log(text.slice(3,7));

let hii = "  hello  ";

// console.log(hii.trim());


function countVowels(str) {
    let vowelCount = 0;
    let vowels = 'aeiouAEIOU';

    for(let i = 0; i< str.length; i++){
        let character = str.chartAt(vowels);

        if(countVowels.includes(character)){
            return vowelCount
        }
    }
}
